@model Recipe

<div class="col-sm-3 col-lg-3 col-md-3 text-center">
    <a asp-controller="Recipe" asp-action="Details" asp-route-id="@Model.Id">
        <img style="width: 200px; height: 200px;" alt="@Model.Name" src="@Model.RecipeThumb" />
        <h4>@Model.Name</h4>
    </a>
</div>
